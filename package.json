{
  "name": "shadcn-chat-svelte",
  "devDependencies": {
    "motion-start": "^0.1.3",
    "prettier": "^3.5.2",
    "prettier-plugin-svelte": "^3.3.3",
    "turbo": "^2.4.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.2",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "check-types": "turbo run check-types"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "docs": "workspace:*",
    "svelte-sonner": "^0.3.28"
  }
}
